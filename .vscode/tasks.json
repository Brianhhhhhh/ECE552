{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run Current File with Dependencies",
            "type": "shell",
            "command": "iverilog -o output ${workspaceFolder}/*.v && vvp output",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
